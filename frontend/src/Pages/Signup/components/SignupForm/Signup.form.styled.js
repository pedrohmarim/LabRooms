import styled from "styled-components";
import { Form, Button } from "../../../../antd_components";

export const FormItem = styled(Form.Item)`
  margin-bottom: 10px;
`;

export const StyledButton = styled(Button)`
  width: 100%;
  height: 45px;
`;
